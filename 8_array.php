<?php
/**
 * Program PHP kedelapan = array  
 * Mengetahui menggunakan array pada PHP 
 * 
 * Array
 * Penulisan array bisa dalam dua bentuk
 *      variabel_array = array(isi)
 *      variabel_array = [isi]
 * 
 * Untuk output array ditampilkan menggunakan print_r(array)
 * Untuk menampilkan beberapa array, menggunakan echo $array[nomor_array]
 * 
 * */

 $array_data = array('satu', 'dua', 'tiga');
 $array_data2 = ['one', 'two', 'three'];

 // print semua array
 // print_r($array_data);

 // echo beberapa array
 // echo $array_data[0];
 echo $array_data2[0];

?>